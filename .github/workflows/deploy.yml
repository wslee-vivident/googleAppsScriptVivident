name: Deploy to Google Apps Script

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Clasp
        run: npm install -g @google/clasp

      - name: Create .clasprc.json
        run: echo '${{ secrets.CLASPRC_JSON }}' > ~/.clasprc.json

      # 👇 [수정됨] 디버깅 정보를 출력하고 에러 발생 시에도 멈추지 않고 원인을 보여줌
      - name: Push all projects
        run: |
          echo "📂 현재 디렉토리 파일 목록 확인:"
          ls -la
          
          echo "🚀 하위 폴더 탐색 시작..."
          
          # 하위 디렉토리를 순회
          for dir in */; do
            # 디렉토리 이름에서 끝의 / 제거 (ex: project/ -> project)
            dirname=${dir%/}
            
            echo "-----------------------------------"
            echo "🔍 Checking folder: $dirname"
            
            if [ -f "$dirname/.clasp.json" ]; then
              echo "✅ .clasp.json 발견! 배포를 시작합니다."
              
              cd "$dirname"
              
              # 실제 배포 명령 (실패 시 에러 메시지 출력)
              clasp push --force || { echo "❌ $dirname 배포 실패!"; exit 1; }
              
              cd ..
            else
              echo "⚠️ $dirname 폴더에 .clasp.json이 없습니다. 스킵합니다."
            fi
          done
        shell: bash