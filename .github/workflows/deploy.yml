name: Deploy to Google Apps Script

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Clasp
        run: npm install -g @google/clasp

      # 4. (수정됨) 더 안전한 방식으로 인증 파일 생성 및 확인
      - name: Create .clasprc.json
        env:
          CLASPRC_SECRET: ${{ secrets.CLASPRC_JSON }}
        run: |
          # 환경변수를 이용해 파일 생성 (따옴표 문제 방지)
          echo "$CLASPRC_SECRET" > ~/.clasprc.json
          
          # 파일이 잘 만들어졌는지 확인 (보안상 내용은 출력하지 않고 길이만 체크)
          if [ -s ~/.clasprc.json ]; then
            echo "✅ .clasprc.json 파일 생성 성공 (파일 크기: $(wc -c < ~/.clasprc.json) bytes)"
          else
            echo "❌ .clasprc.json 파일이 비어있습니다! Secret 설정을 확인하세요."
            exit 1
          fi

      # 👇 [수정됨] 디버깅 정보를 출력하고 에러 발생 시에도 멈추지 않고 원인을 보여줌
      - name: Push all projects
        run: |
          echo "📂 현재 디렉토리 파일 목록 확인:"
          ls -la
          
          echo "🚀 하위 폴더 탐색 시작..."
          
          # 하위 디렉토리를 순회
          for dir in */; do
            # 디렉토리 이름에서 끝의 / 제거 (ex: project/ -> project)
            dirname=${dir%/}
            
            echo "-----------------------------------"
            echo "🔍 Checking folder: $dirname"
            
            if [ -f "$dirname/.clasp.json" ]; then
              echo "✅ .clasp.json 발견! 배포를 시작합니다."
              
              cd "$dirname"
              
              # 실제 배포 명령 (실패 시 에러 메시지 출력)
              clasp push --force || { echo "❌ $dirname 배포 실패!"; exit 1; }
              
              cd ..
            else
              echo "⚠️ $dirname 폴더에 .clasp.json이 없습니다. 스킵합니다."
            fi
          done
        shell: bash